apiVersion: v2
name: gr8
description: Grupp GR8 — Chas Challenge full-stack deploy (api + frontend + mssql)
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: redis
    version: 18.6.0
    repository: https://charts.bitnami.com/bitnami
    condition: redis.enabled
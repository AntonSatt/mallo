apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ .Release.Name }}-api-strip-prefix
  labels:
    {{- include "gr8.labels" . | nindent 4 }}
spec:
  stripPrefix:
    prefixes:
      - /api
